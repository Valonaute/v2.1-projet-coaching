{% extends 'base.html.twig' %}

{% block content %}
<div class="container-fluid">
    <div class="row">
        <div class="col-6">
            <img src="{{ asset('uploads/')}}{{ product.0.image }}" class="img-fluid">
        </div>
        <div class="col-6">
            <h1>{{ product.0.title }}</h1>
            <p>Prix : {{ product.0.price }} €<p>
            <p>{{ product.0.description }}</p>
            <a href="{{ path('cart_add', {id: product.0.id })}}" class="btn btn-primary"><i class="fas fa-shopping-cart"></i> ajouter au panier</a>
        </div>
    </div>
</div>
{% endblock %}
